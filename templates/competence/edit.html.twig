{% block title %}
    {{parent()}} - Modification d'une compétence
{% endblock %}

{% block body %}
    <h2>Modifier la compétence {{editCompetence.name}}</h2>
    {# {'method' : 'POST'}, #}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
