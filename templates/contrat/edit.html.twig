{% block title %}
    {{parent()}} - Modification d'un contrat
{% endblock %}

{% block body %}
    <h2>Modifier le contrat {{editContrat.name}}</h2>
    {# {'method' : 'POST'}, #}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
